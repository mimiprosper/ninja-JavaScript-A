const area = 7.7;
const perimeter = 7.2;

console.log(Math.round(area), Math.round(perimeter));
console.log(Math.floor(area), Math.floor(perimeter)); 
console.log(Math.ceil(area), Math.ceil(perimeter)); // opposite of floor

console.log(Math.random()); // random numbers between 1 & 0
console.log(Math.random() * 100); // random numbers between 1 & 100