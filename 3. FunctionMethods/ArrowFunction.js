const calArea = radius => {
    return 3.14 * radius**2;
};

const bill = (products, tax) => {
    let total = 0;
    for (let total = 0; total < products.length; total++) {
        total += products[i] + products[i] * tax;
    }
    return total;
};
